for h = 1:0.01:50;
x = 2.^(-h);
y1 = (exp(1) + exp(-1)) / 2;
y2 = ((exp(1 + x) - exp(-1 - x)) - (exp(1 - x) - exp(x - 1))) / (4 * x);
y = y1 - y2;
loglog(x,abs(y));
hold on;
end;
title('Figure 7');
xlabel('h = [2^-50, 2^-1]');
ylabel('y = (e^x + e^-x)/2');
